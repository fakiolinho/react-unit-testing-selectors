import React from 'react';

import './style.scss';

const Header = () => (
  <header>
    <nav>
      <span className="logo">React Quick Starter Kit</span>
    </nav>
  </header>
);

export default Header;
